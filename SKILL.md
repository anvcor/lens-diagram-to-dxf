---
name: lens-diagram-to-dxf
description: "把镜头结构图（厂商官网/说明书里的 Lens Construction 图，PNG/JPG/SVG）反推成逐面数据：自动找光轴、分出每片镜片、按圆心落在光轴上做圆弧拟合，输出 Excel 面数据表（面序号/面型/曲率半径/THICKNESS/玻璃占位 BSC7/HR·ED·ASPH 标记/半口径）+ AutoCAD DXF（光轴、各面 ARC、边缘线、光阑、标注，原图按比例贴底）+ 可选最简 CODE V .seq。凡是用户丢一张镜头剖面/结构图并想要「画成 CAD」「转 dxf」「读出半径/厚度」「做成表格/excel」「反推镜头数据」「描一下这个镜头」，或提到 55EVO 这种从图反推的历史项目，都用这个 skill。专利里的数据表格提取不归这里（那是 patent-lens-to-excel）。"
---

# 镜头结构图 → DXF + Excel

两条命令、一次目检、一次交付。核心是 `scripts/diag_extract.py`（图 → JSON + 叠加核对图）
和 `scripts/diag_build.py`（JSON → xlsx / dxf / seq）。

用户的历史做法（55EVO）：AutoCAD 里把说明书图贴进去 → 手动画光轴 → 每个面画一段圆心在光轴上的 ARC →
上下边缘连线 → 标总长/口径 → 逐面读 R/厚度记进 Excel，玻璃不知道就填 BSC7 占位。
脚本做的就是这套流程的自动化，输出格式也照着来。

## 流程

### 0. 环境
```bash
pip install ezdxf cairosvg -q   # numpy/scipy/PIL/openpyxl 通常已有
S=<skill 目录>/scripts
```

### 1. 拿图、裁图（必须先用 Read 看一眼原图）
- 只保留镜头本体：**去掉图例色块、标题文字、尺寸线、外框**。图上的文字和色块会变成假镜片。
- 四周留 10~20 px 空白，别把最外侧镜片的轮廓切掉。
- 图有外框线（整个图被一圈线围住）会让整幅图被填成一片——脚本会报 `[警告] 填洞后 xx% 的画面都被当成镜片`，这时把 `--crop` 收到框内。
- SVG 直接喂，脚本用 cairosvg 栅格化到 4000 px 宽（`--svg-width`）。厂商 SVG 一般是无描边纯填充，效果最好。
- 分辨率决定能分辨的最小空气间隔：约 **3 px**。0.15~0.2 mm 的间隙在 <20 px/mm 的图上会被当成胶合——
  脚本对"两侧拟合半径不一致（符号相反或差 >30%）"的胶合面会自动改判为极薄空气间隔并打印 `[提示]`，但仍要目检。
- 物方在右边的图加 `--flip`。
- **带透明通道的 PNG 必须先合成到白底**：直接 `convert('RGB')` 会把透明区变成纯黑，整幅图的背景就成了
  "黑色"，后面全错（实测适马 85 那张，四角都是 (0,0,0)、bbox 占满全图）。判据：`im.mode` 是
  `RGBA/LA/P` 就 `alpha_composite` 一层白底再用。`fit_check.py` 已经内置这步。
- **光阑常常只画成上下两段短竖线**（不贯穿），脚本按"高度 ≥30% 最高镜片"找竖条会漏掉。
  自己找：暗色连通域里 `宽 ≤8 px 且 40 < 高 < 600 px` 的那两段，x 即光阑位置，
  内侧端点到光轴的距离即光阑半口径。之后按「注入光阑」那段手工插进 surfaces。

### 2. 比例尺（问用户一次，别猜）
三选一：`--length 首面顶点到末面顶点(mm)` / `--diameter 最大镜片直径(mm)` / `--pxmm`。
用户通常知道总长或前镜片直径；都不知道就不给，输出按 1 px = 1 单位，在交付时说明 Excel 里乘个系数即可。
（注意 --length 是**首面顶点到末面顶点**，不含滤光片以外的东西；55EVO 的 75.21 就是这个定义。）

### 3. 提取 + 目检
```bash
python3 $S/diag_extract.py diagram.png -o lens.json --crop WxH+X+Y --length 75.21 \
    --overlay ov.png --debug dbg --tags "1=ED,5=HR,3=ASPH"
```
stderr 会依次打印：颜色类中心（`[颜色]`，类号用于 `--tags`/`--outline`）→ 线条类 → 光轴线 → 每个分片 `[分片k] 色类 x范围 半高` → 面表。
**必须 Read 两张图**：`ov.png`（品红=拟合弧，黄点=顶点，绿=光阑，红=光轴）和 `dbg_parts.png`（每片一色）。核对：
- 分片数 = 图上镜片数？多了 → 看是哪块（图例、文字、被描边围起来的空气间隔）用 `--drop 序号` 去掉；
  少了 → 两片粘在一起，多半是描边有断口：`--line-dilate 1`（会牺牲极薄间隔）或调 `--k`。
- 光轴红线穿过所有镜片的对称中心？不对就 `--axis <像素y>`。
- 品红弧贴着轮廓？rms > 1 px 的面会标「可能非球面」——真非球面拟合值只是近似，Excel 里会写「非球面」并备注。
- 背景色分片可能是"未填充的镜片"，也可能是"两片镜片之间被围起来的空气"。脚本按**边缘**自动判：
  镜片一定画出有限厚度的机械边缘（最上/最下一行有实宽），空气楔两端是两条面弧的交点，是真正的尖点（1~3 px）。
  判为空气的会打印 `[丢弃] … 上下两端是尖点`；判错了用 `--keep-bg` 全保留、或 `--drop` 手点。
  **彩色分片一律当玻璃**，不参与这条判定。
- 空心镜片 + 填充镜片混排（像 55EVO 截图、星曜 35APO 的黑底白描边图）都能处理。

标记：厂商图例里的颜色（ED/HR/ASPH/萤石…）对应 `[颜色]` 里的类号，用 `--tags` 写进去；不确定就先不给，看 `dbg_parts.png` 再补。

### 4. 建表出图
```bash
python3 $S/diag_build.py lens.json -o 55EVO --image diagram_crop.png --title "AF 55mm F1.8" --seq
```
- `55EVO.xlsx`：`面数据` 表（面序号/面型/曲率半径/THICKNESS/玻璃/标记/半口径/拟合RMS/备注，STO 行 Infinity，IMA 行 =SUM 总长）+ `元件` 表。
- `55EVO.dxf`：mm；图层 AXIS/SURF/EDGE/STOP/DIM/IMG；首面顶点为原点；`--image` 会把裁好的图按比例贴在 IMG 层，
  用户在 AutoCAD 里能直接对着底图核对。**贴图的文件要和 dxf 放同一目录、且文件名必须纯 ASCII**
  （DXF 里存的是相对文件名；中文名会被 AutoCAD 读成 `??`，见「踩过的坑」）。
- `--seq`：最简 CODE V 序列（BSC7_HOYA 占位、CIR 口径、STO），`in` 进 CODE V 就能看 layout。
- `--spec`：patent-lens-to-excel 的 spec.json（只到面表这一层，没有 zmx 块；要 .zmx 需再补玻璃）。

### 4.5 手工补光阑 / 补标记
脚本漏掉的光阑、以及**描边颜色**（而不是填充色）表示的特殊玻璃，都要手工补进 `lens.json` 再 build：

```python
# 光阑：插在对应的空气隙里，并把前后两段 D 重算
i = next(k for k,s in enumerate(S) if s['vertex'] > SX)
assert not S[i-1]['glass']
S[i-1]['D'] = round((SX - S[i-1]['vertex'])/px, 3)
S.insert(i, dict(R=None, R_mm=None, vertex=SX, semi=SSEMI, semi_mm=round(SSEMI/px,2),
                 semi_fit_mm=round(SSEMI/px,2), rms=0.0, glass=False, color=None,
                 stop=True, D=round((S[i]['vertex']-SX)/px, 3)))
```
`--tags` 只能按**填充色**打标记。描边色标出来的元件（适马那张的红色描边）填充色是背景，
要先用颜色掩膜找出红色连通域的 x 范围，再按顶点匹配给对应的面写 `s['tag']`。
插完 `sum(D)` 要等于给定总长，这是个免费的校验。

### 5. 交付
SendUserFile + `device_commit_files` 到用户文件夹（历史项目都在 `E:\Download\`）。回复只说：面数、总长、比例尺、
哪几面标了非球面/需人工核对、哪几处是自动改判的极薄间隔。玻璃一律 BSC7 占位，明说。

## 算法（改脚本前先读这段）
1. k-means 6 类颜色 → 合并相近中心；最大类=背景；3×3 开运算能抹掉 >60% 像素的类=线条类（描边、光阑、光轴线、文字）。
2. 线条 3×3 闭运算接断点（**不膨胀**，膨胀会吞掉 5 px 以内的空气间隔）→ 填洞得到镜片整体 `body`。
3. `body` 内去掉线条，按颜色类分连通域 = 分片；占所在镜片整体 <5% 的碎片（抗锯齿过渡带）并入邻片；
   横向贯穿的长线判为光轴线，只在它所在的几行做竖向闭运算把上下两半接回去（别在全图做——斜的分隔弧会被桥掉）。
4. 描边像素按距离变换归还给最近分片，**只归还 ≤2.5 px 的**（否则光轴线会顺着空气间隔把轮廓拖出去）。
5. 每片逐行取最左/最右像素 → 左右轮廓 → **稳健拟合**（见 3.7）：以靠近光轴的一半当种子起拟一个
   圆心约束在光轴上的圆（`x²+y'² = 2·cx·x + (R²−cx²)`，线性解），再逐轮把符合它的点吸收进来。
   顶点取圆上离轮廓更近的那一支；入选点的 x 跨度 < `--flat-span` 判平面。
   入选点的最大半径记为 `semi_fit`，就是这个面在图上真正画到哪儿为止。
6. 胶合判定：两顶点距 <2 px **且**两顶点之间靠近光轴的几行没有背景像素 **且**两侧拟合半径一致。胶合面取两弧平均。
7. 光阑：宽 ≤ max(4, 2.5% 最高镜片高) 且高 ≥30% 的竖条；落在玻璃内的忽略并告警。

### 3.5 描边就是透镜边缘本身，真实面在**描边中心线**上
这是最容易做错、也最致命的一条（用户当场打回过）：白描边不是"两片之间的分隔物"，
它**就是透镜的轮廓线，只是画得粗**。所以：

- 面的位置 = 描边的**中心线**，不是填充区的内边。取填充区内边的话，每片都瘦掉一个线宽，
  相邻两片之间还会凭空多出一个 ≈0.2 mm 的假气隙，在 CAD 里表现为**口径打架**。
- 脚本用两遍归属实现：第一遍照常分片；第二遍**只拿镜片分片当种子**（第一遍连白描边自己都是种子，
  各片才会停在填充边上），每片按 `描边宽/2` 外扩，且**只许长进描边像素，不许跨过黑色空气**。
  两片共用一条描边时，最近邻正好把它从中间劈开 → 两片恰好相接；两条独立描边则各自停在自己的中心线上。
  （跨过空气去长的话，分界会跑到气隙正中间，实测把一个真平面拉成了 R=−121 mm。）
- `[描边宽] N px` 之后，D 仍然小于它的面会标 `★两面在轴上几乎相接`：这是图纸分辨力的下限，
  真实设计应有 0.05~0.2 mm 气隙，交付时要点出来。

### 3.6 作图半径要做穿透截断，否则就是「口径打架」
相邻两面各自延伸到自己的机械半径时会互相穿透（边缘厚度变负），CAD 里就是两片镜子的轮廓交叉成 X。
`diag_build.py` 会对每一对相邻面二分求出"轴向间距归零"的半径，把两面的**作图半径**压到它之下，
写进 Excel 的 `作图半径` 列并在备注里说明；`半口径` 列仍是从图上量到的机械半径，不动。
**不要**再把"同一片的前后两面"强行取齐——胶合面是两片共用的，取齐会顺着胶合链把整组压到最小值
（实测绿片从 12.09 被拖到 9.88）。半径不等时画台阶就行。

### 3.7 别用「x 不再变化」去砍磨边 —— 会把真实弧砍掉，半径直接错 17%
最早的 `trim_rim()` 判据是「离轴 >60% 处连续 4 行 x 变化 <0.5 px 就当机械磨边平台砍掉」。
**这个判据对弱曲率面根本不成立**：dx/dy = y/√(R²−y²)，R=840 px、半高 280 px 时边缘处才 0.33 px/行，
于是 40% 的真实弧被当成磨边砍了。剩下那 60% 矢高只有 15 px，噪声占比大，拟合就飘：
实测 E7 后表面真值 ≈840 px，砍完拟成 996 px（**R 偏 17%**，用户一眼看出"球面镜偏差这么多"）。

现在改成**种子 + 生长**：靠近光轴的一半一定是真实面，拿它起拟；再把落在容差内的点逐轮吸收。
机械磨边是一段横平边，离圆很远，自然留在外面当离群点。`--flat-span` / `seed_frac` / `tol_min` 可调，
但实测这三个参数对结果几乎没影响（整体误差 0.81~0.84 px），说明已经到图的分辨极限了。

**作图半径用 `semi_fit`，不要用机械半径**（元件外框半高）：后者会让弧冲出这个面实际存在的范围，
在 CAD 里压进邻片。`半口径` 列仍报机械半径，`作图半径` 列报 `semi_fit`（再经 3.6 的穿透截断）。

## 踩过的坑
- 截图型输入（AutoCAD 里 CAD 线叠在 jpg 上）是双层轮廓，两条线之间会多出一条薄分片——`--drop` 掉；真正的输入应是干净的厂商图。
- 用户历史 DXF 的边缘线把某些空气间隔也围成了闭合区域（如 55EVO 面 3→4 的 6.63 mm 空气），线稿里它和空心镜片长得一样，
  只能目检后 `--drop`；中心厚 <0.4 mm 的这类区域脚本会自动丢（`--min-thick`）。
- 弱曲率面（|R| > 150 mm）从图上只能拟到 ±15%，交付时点明。
- **近乎平面的面会在噪声里拟出一个假圆**：轮廓 x 跨度只有 2 px（量化噪声）时最小二乘照样给出 R=-3000 px，
  还附带 rms 1.5 而被误标"可能非球面"。`--flat-span`（默认 2.5 px）把这种面直接按平面报——
  报一个没有数据支撑的大半径比报平面更不诚实。
- 半口径按元件整体高度给（前后两面相同）；机械口径含磨边，比通光口径大 0.5~1.5 mm。
- **ezdxf 的 `EZDXF` 标注样式自带 `dimlfac=100`（给「米 + 1:100」用的）**，不覆盖的话 mm 图上 50 mm 会标成 5000；
  `dimdsep` 不给还会把小数点吞掉。`diag_build.py` 已经在 override 里压成 `dimlfac=1, dimdsep='.'`，别删。
- **贴图在 AutoCAD 里「不可读」——根因是 ACAD_IMAGE_DICT 的键**。查了三轮才定位，过程值得照抄：
  1. 现象：外部参照面板里图片状态是「不可读」，`找到位置` 那一行却是对的 —— 说明**路径没问题，是解码/加载失败**。
  2. 先排除文件损坏：把设备上那份图拉回来比 MD5，并检查 PNG 结构（8 位真彩、无隔行、无 iCCP）。完全正常。
  3. 拿**用户手上一份 AutoCAD 自己写的、贴图能正常显示的 DXF 当标准答案**（这里是 55EVO.dxf），
     逐字段比对 CLASSES / IMAGEDEF / IMAGE / IMAGEDEF_REACTOR / RASTERVARIABLES / **ACAD_IMAGE_DICT**。
  4. 还不确定就让用户手动 `IMAGEATTACH` 同一张图再保存，然后把保存后的文件拉回来 —— 这样就有了
     「同一张图、同一个 AutoCAD、能用」的对照组，差异一目了然。
  **真凶**：`ACAD_IMAGE_DICT` 的键必须是**去掉路径和扩展名的纯名字**（AutoCAD 写的是 `35APO_base`、
  `AF 55mm F1.8 EVO FE 说明书-3`）。ezdxf 的 `add_image_def()` 默认**拿整个文件名当键**，于是键里带上了
  `.\` —— 反斜杠在 AutoCAD 符号名里非法，注册不进字典，AutoCAD 打开时把引用它的 IMAGE 实体**整个丢掉**。
  必须显式传 `name=os.path.splitext(basename)[0]`。
  顺带修掉的几处（都照 55EVO 对齐）：`filename` 带 `.\` 前缀；`IMAGEDEF` 的 `pixel_size = 1/图宽`；
  `IMAGE` 的 `flags=7`、`fade=50`；`set_raster_variables(frame=1, quality=1, units='mm')`（ezdxf 默认 'm'）；
  `u_pixel`/`v_pixel` 写死成正交（`add_image` 算出来会带 1e-19 的脏分量）；
  **组码 290 要挪到 283 之后、360 之前** —— DXF 手册把它列在最后、ezdxf 照手册写，但 AutoCAD 自己写在中间
  （`_fix_image_group_order()` 在 saveas 之后做文本重排）。
- **中文文件名其实是能用的**（AutoCAD 自己写的 55EVO.dxf 外参名和字典键都是中文）。但 **ezdxf 写出来的不行**：
  它按 UTF-8 写而 DXF 头里声明的是 ANSI 码页，AutoCAD 读成 `??` → 外参「未找到」，画面上只剩一行灰色占位文字。
  所以我们这边一律用 ASCII 名，`diag_build.py` 会自动转。
- **AutoCAD 的光栅引擎读 PNG 不如读 JPEG 稳**，所以贴图一律转 JPEG（`--image-keep` 可关）。
  （注：本例最终定位到的是字典键，不是格式；但 JPEG 这条保留，成本为零。）
- 厂商图例说「双面非球面」的，用 `--tags` 写进去，脚本会把该元件**前后两个面**都定成非球面，
  不再只靠拟合残差（暗红这种强弯月片残差常常 <1 px，光看残差会漏判）。表里的 R 是最佳拟合球面，只能当初值。

## 验证记录
- 55EVO.dxf 渲染成线稿回灌：23 面全部找到，R 误差 1~3%（弱曲率面除外），厚度误差 ≤0.05 mm，STO 位置误差 0.05 mm。
- 55EVO AutoCAD 截图（空心+填充混排，17 px/mm）：同上，0.15 mm 间隔靠半径一致性检查救回。
- 合成的厂商风格 SVG（纯填充无描边）：全部命中，光阑识别正常。
- 星曜 APO M 35mm F1.7 ASPH. 官方结构图（黑底 + 白描边 + 三种填充色，25 px/mm，总长 50 mm）：
  11 片 18 面全部命中；两处「被两片镜片夹住的空气楔」（4.45 / 6.57 mm）靠尖点判据自动剔除；
  第 14 面确认是真平面（逐行 x 恒定）。
  **逐面量化核对**（把拟合弧压回原图，逐行比到描边中心线）：全部面平均偏差 **0.82 px ≈ 33 µm**，
  单面最差 1.8 px。换稳健拟合前，面 11 的 R 偏 17%、整条弧系统性偏 2.5 px。
  这个「压回原图逐行量偏差」的脚本（`scripts/fit_check.py lens.json crop.png`）是验收的硬指标，
  比看叠加图可靠得多 —— **交付前必跑**，全局平均 < 1 px 算合格。
- SIGMA 85mm F1.2（白底线稿 + 蓝填充 + 红描边，26.5 px/mm，总长 119.72 mm）：
  17 片 29 面 + 手工补的光阑，全部面平均 **0.66 px ≈ 25 µm**；sum(D) 回算 119.721 mm。
  源图是带 alpha 的 PNG，直接转 RGB 会整幅变黑（见 §1）。
